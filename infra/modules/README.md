# infra/modules

Cohesive, reusable Terraform modules (e.g. naming, tags, resource-group, networking,
storage, monitoring). Create a module only for real reuse or an architectural
boundary — not one per resource. Empty until needed.
