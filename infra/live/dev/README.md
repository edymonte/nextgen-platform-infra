# infra/live/dev

DEV environment compositions, each with its own remote-state key:
`platform/`, `connectivity/`, `shared-services/`, `workload/`.
Separate states, small blast radius. Empty until TASK-AZ-007.
